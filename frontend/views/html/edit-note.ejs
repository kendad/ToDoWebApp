<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="../css/input_form.css">
    <title>TO-DO-APP</title>
</head>
<body>
        <div id="updateNoteModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg">
              <div class="modal-content modal-background">
                <div class="d-flex justify-content-center">
                  <img src="../icons/scroll.png">
                  <h1 class="h1-text">UPDATE-NOTE</h1>
                </div>
                <div class="d-flex justify-content-center">
                  <div class="d-flex flex-column justify-content-center input-form-container">
                    <form action="/edit-note/<%= jsonData.id %>/" method="POST">
                      <label for="title" class="label-text">Title:</label>
                      <input class="input-text" type="text" id="title" value="<%= jsonData.title %>" name="title">
                      <br>
                      <br>
                      <br>
                      <label for="description" class="label-text">Description:</label>
                      <textarea class="input-text" type="text" id="description" name="description"><%= jsonData.description %></textarea>
                      <br>
                      <br>
                      <br>
                      <label for="collab" class="label-text">Section:</label>
                      <div class="add-user-container">
                        <select class="form-select form-select-design" aria-label="Default select example" id="collab" name="section">
                          <% if (jsonData.section===1){ %>
                            <option value="1" selected>Next Up</option>
                          <% }else{ %>
                            <option value="1" >Next Up</option>
                          <% } %>

                          <% if (jsonData.section===2){ %>
                            <option value="2" selected>In Progress</option>
                          <% }else{ %>
                            <option value="2" >In Progress</option>
                          <% } %> 

                          <% if (jsonData.section===3){ %>
                            <option value="3" selected>Completed</option>
                          <% }else{ %>
                            <option value="3" >Completed</option>
                          <% } %>   
                          
                        </select>
                        </div>
                      <div class="d-flex justify-content-center">
                        <button type="submit" class="input-button-text">UPDATE</button>
                      </div>
                    </form>
                    <div class="d-flex justify-content-center">
                        <form action="/delete-note/<%= jsonData.id %>" method="POST">
                            <button type="submit" class="input-button-text">DELETE</button>
                        </form>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>


          <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>